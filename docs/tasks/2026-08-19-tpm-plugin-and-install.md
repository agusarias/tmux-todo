# TPM Plugin And Install

**Status:** draft
**Worktree:** none

## Goal
Ship the TPM plugin that wraps the Go binary — install the keybind and rename hook, resolve
the `tdo` binary, honour `@todo-key` — plus the README covering install, configuration, and
the sesh integration.

**Design:** docs/design.md — "Distribution"
