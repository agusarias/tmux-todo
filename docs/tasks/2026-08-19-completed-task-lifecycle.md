# Completed Task Lifecycle

**Status:** draft
**Worktree:** none

## Goal
Completing a task with `space` leaves the row visible and struck-through, then purges it
from the view on popup close or after 24h — whichever comes first. The row stays in the DB
marked done so history and stats remain possible later.

**Design:** docs/design.md — "Completion vs deletion"
